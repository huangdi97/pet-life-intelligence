# LIMITATIONS — v0.1 已知限制（诚实清单）

## 医疗安全

1. 红旗规则为关键词匹配（zh/en），覆盖 8 类紧急模式 + 2 类警示模式；不认识
   同义改写/错别字/纯口语描述时可能漏报。规则引擎每条规则有正/反例测试，
   但不能替代兽医。UI 在健康页面始终提示分级来自规则引擎。
2. 无体温/心率等设备数据接入（DEVICE_DERIVED 预留未用）。
3. 不做剂量推荐、不自动建议停药、不下诊断——设计边界，非缺陷。

## 认证与安全

4. 仅 dev 认证（签名 cookie/header），无密码/OIDC/MFA（v0.2, PLI-217）。
5. 速率限制为进程内实现，多实例部署需 Redis 化。
6. 病毒扫描边界未实现（上传只做 MIME+魔数+大小校验）。
7. 分享链接（Care Card/Vet Brief）是无鉴权 JSON 端点：靠 token 熵、过期、
   撤销与访问审计保护；不含完整医疗历史。

## 平台

8. Worker 为轮询循环，无 durable queue；关键状态只存 PostgreSQL。
9. Timeline 无分页游标缓存优化（limit 上限 200）。
10. 今日页时区固定按宠物时区 +08:00 处理（pet.timezone 字段已预留）。
11. Playwright 浏览器级 E2E 未运行（7 条主路径为 API 级 E2E 覆盖）。
12. 通知为站内列表；无邮件/推送通道。
13. 单文件上传接口（非 presigned 直传）；MinIO 不可用时自动降级本地磁盘。

## 产品

14. 商业功能（支付/商城/Marketplace）按冻结范围完全未实现。
15. RAG 未引入向量检索（按 GOAL 用结构化 DB 查询）；pgvector 镜像已就位。
16. Vet Brief 分享为 JSON 端点，PDF 导出未实现。
