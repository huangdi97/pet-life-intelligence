## Goal

在 `E:\AI\Pet Life Intelligence`（HEAD=df05c9d、branch=main、无 remote、worktree 非干净——含已修改的 `AGENTS.md`（+1002 行）与 2 个未跟踪 goal 文件，全部保留、不删除，并纳入本轮 `[PLI-STAGEV2]` 提交）执行 **Stage V.2**：对全仓所有人工维护代码（`services/api`、`services/worker`、`services/ai-gateway`、`packages/*`、`apps/*` 生产源码 + 关键测试/脚本）进行逐文件审核、修改、拆分、清理、补注释、补类型、补测试与工程规范收口。

这不是"再跑一次 ruff / 再跑一次 pytest 然后宣布质量好"：每一个文件必须有本轮处置结论与真实命令证据，规模、类型、注释、死代码、测试、回归全部以可复现扫描 + 真实命令收口。行为保持等价（纯重构，不改变 API 契约与业务语义），既有测试不被删除或弱化。

实测基线（本轮全部用真实命令重跑，不沿用历史数字）：pytest 420 / ruff 0 / 五端 typecheck 0 / 五端 build OK / vitest 22/22 / Playwright 29/29。

实测违规清单（拆分对象，作为 baseline 可核对）：
- Python >300 行（14 个）：`api/routes/v10_platform.py` 1141、`api/routes/v02_social_platform.py` 649、`api/routes/care.py` 579、`api/routes/v10_extras.py` 517、`api/routes/visual.py` 487、`api/routes/health.py` 454、`api/routes/v02_care_health.py` 436、`app/seed.py` 390、`domain/event_types.py` 386、`services/auth.py` 386、`models/v02.py` 317、`api/routes/v02_behavior_training.py` 316、`ai-gateway/gateway.py` 311、`api/routes/v02_identity_daily.py` 309。
- React 组件/页面 >200 行（12 个）：`apps/web/app/settings/page.tsx` 382、`apps/mini/src/pages/index/index.tsx` 373、`apps/web/app/page.tsx` 304、`apps/mobile/src/screens/QuickLogScreen.tsx` 295、`apps/mini/src/pages/agent/index.tsx` 277、`apps/web/app/agent/page.tsx` 275、`apps/web/app/timeline/page.tsx` 264、`apps/web/app/companion/page.tsx` 244、`apps/mobile/src/screens/ui.tsx` 243、`apps/web/app/social/page.tsx` 227、`apps/web/app/welfare/page.tsx` 217、`apps/mobile/src/screens/TodayScreen.tsx` 205。
- 非组件 TS >300 行（1 个）：`apps/web/lib/i18n.ts` 316（翻译数据，按职责拆分或按大型 fixture 例外文档化）。

开放账项（用户已确认本轮收口）：
- **SV-006**：`apps/mobile` 双 API 层（`src/api.ts` + `src/services/api.ts`）合并为单一契约层。
- **SV-007**：限流从 in-process deque 迁移为 Redis 分布式实现（`redis-py 8.1.0` 已装、`redis>=5.0` 已声明、docker-compose 已有 redis:7 于 56379），无 Redis 时必须可回退到 in-process，本地测试不硬依赖 Redis。

## Acceptance criteria（全部可客观验证）

### A. 规模与结构收口（可复现扫描）
1. 交付一个可复现的扫描命令/脚本（排除 `node_modules`、`dist`、`.next`、`__pycache__`、`.venv`、`migrations/`、锁文件），输出证明：人工维护生产源码无 >300 行文件、React 组件/页面无 >200 行文件；若存在例外，每例必须落表：文件/行数/类别（仅允许自动生成、迁移、大型静态 fixture）/理由，且例外清单随报告提交。
2. 上述违规清单中的每个文件都有处置记录：拆分后文件清单、各文件行数、模块职责说明；行为等价由回归测试证明。

### B. 全仓逐文件审核报告（交付物）
3. `reports/STAGE_V2_FULL_CODEBASE_REVIEW.md` 存在并覆盖每个人工维护生产文件：审查结论（无需处理 / 拆分 / 重构 / 补注释 / 补类型 / 清理死代码 / 补测试）+ 代表性证据（拆分映射表、新增注释标签、类型修复、删除的死代码清单）。
4. `reports/STAGE_V2_ISSUE_LEDGER.md` 更新：SV-006=CLOSED、SV-007=CLOSED、P0/P1 剩余=0；每行含 ID/Category/Severity/Evidence/File/Status/Fix/Test/Remaining Risk。
5. 注释收口：关键路径（医疗安全 / 权限 / 幂等 / 并发 / 隐私 / 合成隔离 / 3D provenance / provider 降级）补高价值注释（`SAFETY:`/`SECURITY:`/`INVARIANT:`/`PROVENANCE:`/`PILOT-INTEGRITY:`/`PROVIDER:` 标签按 AGENTS.md）；垃圾/过期注释删除；扫描无裸 TODO/FIXME/HACK/XXX/TEMP（或全部带追踪 ID 与移除条件）。
6. 类型收口：全仓 Grep `any` / `unknown as` / `@ts-ignore` / `@ts-expect-error` / `noqa` / `type: ignore` / `cast` 逐个判定，无新增未证明的类型逃逸；五端 typecheck 0。

### C. SV-006 / SV-007 收口
7. SV-006：`apps/mobile` 双 API 层合并为单一契约层（统一引用，删除另一层），`pnpm --dir apps/mobile typecheck` 0。
8. SV-007：限流实现为 Redis 分布式 + in-process 回退的 adapter，配置集中（`rate_limit_enabled` / `rate_limit_per_minute` / backend 选择），Redis 不可用时自动回退且回退有日志与测试；新增限流单元测试覆盖 Redis 路径与回退路径。

### D. 补测试
9. 每个被拆分的模块获得回归覆盖（新增测试或已有测试覆盖其路径）；新增测试按行为命名；`pytest` 总数 ≥ 420（记录实际数字）且 0 failed；禁止删除或弱化既有断言使 CI 变绿。

### E. 全量回归（真实命令重跑，记录 command/date-time/duration/结果）
10. `ruff check services/api services/worker packages/rules services/ai-gateway` → 0 error。
11. `.venv\Scripts\python.exe -m pytest -q` → ≥420 passed / 0 failed。
12. 五端 typecheck（`apps/web|admin|pro|mini|mobile`）→ 0 error。
13. 五端 build（web/admin/pro `build`、mini `build:weapp`、mobile 既有 gate）→ OK。
14. `apps/web` vitest ≥ 22/22；`tests/e2e-browser` Playwright ≥ 29/29（Docker 29.2.1 可用则应完整运行；若会话内引擎确实不可用，如实记录 EXTERNAL_BLOCKED 并附重跑命令，禁止伪造通过）。
15. 安全/医学/合成隔离 invariants 重验：`tests/safety`、`tests/stage_v`（adversarial/properties/time_travel/synthetic_isolation）、`tests/ga` 关键套件全绿；`SYNTHETIC_NEVER_COUNTS_AS_REAL` 保持；Deterministic Red Flag 不被 LLM 降级保持；权限 deny-by-default 与撤销生效保持。

### F. 收口与提交
16. `WORK_STATUS.md` + `CHANGELOG.md` 增加 Stage V.2 状态块（真实证据数字）。
17. 全部本轮修改 + 既有未提交工作（`AGENTS.md` 修改 + 2 个未跟踪 goal 文件）以 `[PLI-STAGEV2]` 前缀提交到 main（消息含 PLI-STAGEV2 与变更摘要）；不 force push、不丢弃用户未提交工作；完成后 STOP，不自行进入 Stage I / v1.3 / Future 42 / 新功能开发。

## Boundaries

- **功能/设计冻结**：FEATURE_FREEZE=ON、PRODUCT_DESIGN_FREEZE=ON。只允许 bug fix / 重构 / 拆分 / 清理 / 注释 / 类型加固 / 测试 / 文档对齐 / 安全加固 / 死代码删除 / 性能清理 / 无障碍修复；不新增 Feature ID、不进入 Stage I / v1.3 / Future 42。
- **Minimum Safe Refactor**：按职责高内聚低耦合拆分，保持行为与 API 契约等价；不做大规模改名、换框架、重写架构、目录大挪移；不删除看不懂的数据或用户未提交工作；migrations 为自动生成例外，不改其历史语义。
- **医疗安全**：不诊断、不改药、不改剂量、不把 AI inference 写成 clinical fact；Deterministic Red Flag > LLM；相关安全 invariants 不得弱化。
- **权限/数据治理**：deny by default、服务端强制、撤销即时生效；合成数据永不进入真实 Pilot 指标；不创建真实 Participant/Pet；不部署公网；不使用真实外部 AI/SMTP key 超出既有配置。
- **SV-007 Redis**：允许使用已在依赖中的 `redis-py` 与现有 docker-compose redis 服务（56379）；无 Redis 时必须可回退，本地测试不硬依赖 Redis 常驻。
- **诚实原则**：任何"完成"必须有本轮真实命令证据；不复制历史数字当最终证据；Docker/外部不可用时如实标 EXTERNAL_BLOCKED；secrets 审计只报告 location/type/severity，绝不输出内容；日志不写完整敏感数据。
- **不通过降低质量 Gate 达成目标**：不删测试、不弱化断言、不加大范围 ignore、不关闭 lint/typecheck 规则；发现根因并修复。